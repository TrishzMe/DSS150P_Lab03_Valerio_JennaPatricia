# Installed on top of apache/airflow:2.10.5-python3.11 (see Dockerfile.airflow).
# Same pipeline libraries as requirements.txt so the DAG runs the identical CLI.
pandas==2.2.3
pyarrow==17.0.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
PyYAML==6.0.2
psycopg2-binary==2.9.9
# Pinned to the Airflow 2.10.5 constraint so pip does not upgrade Airflow's own numpy.
numpy==1.26.4
